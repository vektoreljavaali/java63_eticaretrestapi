server:
  port: 8081

spring:
  datasource:
    platform: postgresql
    url: jdbc:postgresql://localhost:5432/MusteriDB
    username: postgres
    password: root
    driverClassName: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
